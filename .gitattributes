# Normalize line endings: text files use LF in the repo, checked out as-is per platform.
* text=auto eol=lf

# Binary assets — never touch line endings or diff them.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
